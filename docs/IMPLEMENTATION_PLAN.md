# 実装計画書

> 文書状態: 完了済み計画の履歴記録。現行PWAの規範仕様は `PWA_SPECIFICATION.md`、保守運用は `OPERATIONS_RUNBOOK.md` を参照する。

## 1. 目標

1. GitHub Pages上に完全クライアント処理型PWA「神は7日で世界を作った」を構築し、AI人物生成に有効な選択肢を拡充して指示文生成を検証する。
2. 生成した基準指示文を画像生成GPTへ投入し、Codexとユーザーが相談しながら調整し、同一条件3回の画像品質チェックでユーザーの明示的な「合格」を得る。
3. 合格後、旧・指示文生成GPTを非公開化して到達不能を確認し、ユーザーの最終承認後に完全削除する。

## 2. PWAの仕様

- 主端末: iPhone 16。393×852 CSS px相当の縦表示、safe area、44px以上の操作領域、16px以上の入力文字、横スクロールなしを事前検証する。
- 実機確認: Chromiumの端末エミュレーションはレイアウト確認に限定し、iPhone 16のSafariでホーム画面追加、入力、色選択、コピー、再読込、オフライン起動を確認する。
- 処理: ブラウザ内だけで完結し、API、DB、分析、広告、外部フォントを使わない。前回値と名前付きプリセットのみlocalStorageへ保存する。
- UI: 一般項目はプルダウン。色はカラーチップと名前を組み合わせた選択パネル。自由記述と任意HEX入力は設けない。
- 選択肢: 具体的で視覚差があり、画像モデルが解釈しやすく、他項目と組み合わせやすい語句を優先する。
- 任意項目: 表情、ポーズ、構図、背景、照明。未選択なら出力せず、選択時はこの順で末尾へ追記する。
- 公開: ローカル自動試験の成功を確認してから `main` へ反映し、GitHub Pagesのブランチ配信で公開する。

## 3. 指示文検証

- PWA 3.0.0では、衣装未選択の初期出力12行、上下分離17行、上下一体16行が、それぞれ承認済みの規則と完全一致することを自動試験する。
- 必須初期値がカタログ内に存在すること、色名称が定義済みチップと一対一であること、重複がないことを試験する。
- 任意項目の省略と追記順、衣装色と衣類名の合成、外部通信要素の不在、PWA主要資産を試験する。
- 公開後にコピー結果、プリセット、再読込保持、オフライン起動を確認する。

## 4. 画像生成GPTの調整と品質合格

### 優先順位

1. 画像生成GPTのInstructions
2. 画像生成GPTのKnowledge
3. InstructionsとKnowledge間の競合・役割分担
4. PWAの表現力または出力語句が原因の場合だけ、PWAの選択肢、出力語句、項目構造、指示文規則

調整前に、画像生成GPTの名称、説明、モデル、Instructions、Knowledge、機能、会話のきっかけ、共有状態を読み取り確認する。InstructionsとKnowledge本文は公開リポジトリへ保存せず、非公開領域にバックアップする。公開ログには版、ハッシュ、変更目的、要約だけを記載する。

### Codexとユーザーの共同調整

1. PWAで基準指示文を生成する。
2. 画像生成GPTの独立した新規会話へ投入する。
3. ユーザーが画像を目視し、気になる点や期待との差を伝える。
4. Codexが差異を人物、人体、衣装、色、髪、瞳、構図、フォトリアル性に整理する。
5. Codexが原因をInstructions、Knowledge、両者の競合、PWAのいずれかに診断する。
6. Codexが修正候補、期待効果、副作用、影響範囲を提示する。
7. ユーザー承認後、原則として画像生成GPTのInstructionsまたはKnowledgeを一変更単位ずつ更新する。
8. PWAが原因の場合だけ、選択肢、出力語句、項目構造または指示文規則を更新する。
9. GPT変更時は設定と共有状態を再確認し、PWA変更時は自動試験と再公開を行う。
10. 独立した新規会話で再生成し、ユーザーが納得するまで反復する。

### 合格条件

安定した同一指示文を、同一PWA版・同一GPT設定版で、履歴のない独立した新規会話3件へ各1回投入する。3画像をユーザーが目視し、最終的に明示的な「合格」を宣言した時点で完了とする。1件でも不合格なら調整サイクルへ戻る。

## 5. 旧GPTの停止と削除

1. PWA自動試験、Pages公開、iPhone実機確認、画像生成GPT調整、3画像合格を完了する。
2. 旧・指示文生成GPTを非公開化または共有停止する。
3. 旧リンクから利用できないことを確認してユーザーへ報告する。
4. 永久削除についてユーザーの明示的な最終承認を得る。
5. 承認後に完全削除し、一覧からの消失と旧リンクの無効化を確認する。

既存の会話履歴や生成画像は削除対象に含めない。

## 6. 記録

- `docs/IMAGE_QUALITY_LOG.md`: ケースID、PWA版、GPT設定版、ハッシュ、指示文、ユーザー指摘、診断、承認済み変更要約、再生成結果、判定。非公開Instructions/Knowledge本文は記録しない。
- `docs/RETIREMENT_LOG.md`: 停止時刻、到達不能確認、削除承認、削除時刻、最終確認。

### 公開記録への記録禁止事項

公開リポジトリ内の計画書、品質ログ、停止・削除ログ、改善要望記録、README、Issue、Pull Request、コミットメッセージを含むすべての公開記録には、次の情報を記録しない。

- 実際のIPアドレス、SSID、端末内パス
- 認証情報、Cookie、トークン
- 非公開GPTのInstructions・Knowledge本文
- 非公開GPTの編集用・共有用リンク
- ユーザーの個人情報を含む生のフィードバック
- 添付画像の保存先や個人情報を含む画像

必要な記録は、個人を特定できない要約、版、ハッシュ、一般化した試験条件、判定に限定する。公開前に上記情報が含まれていないことを確認する。

## 7. 承認ゲート

- 画像生成GPTの設定変更は、変更案と影響を提示してユーザー承認後に行う。
- 画像品質合格はユーザーの目視と明示的な「合格」に限る。
- 旧GPTの永久削除は、非公開化と到達不能確認後に別途明示承認を得てから行う。

## 8. フェーズ構成と現在地

| フェーズ | 内容 | 状態 |
|---|---|---|
| 0 | 計画策定・画像生成GPTの現行設定バックアップ | 完了 |
| 1 | PWAの設計・構築・AI人物生成に有効な選択肢の拡充 | 完了 |
| 2 | 自動試験・GitHub Pages公開・iPhone 16実機Safari確認 | 完了 |
| 3 | PWA 3.0.0で基準指示文を確定し、画像生成GPTの独立した新規会話で初回画像を生成 | 完了 |
| 4 | 生成結果をユーザーと評価し、画像生成GPTまたは必要な場合のみPWAを共同調整 | 完了。PWA 4.0.1の公開とiPhone 16実機Safari確認まで合格 |
| 5 | 同一PWA版・同一GPT設定版・同一指示文を独立した新規会話3件で生成し、最終品質を確認 | 完了。第3サイクル3件をユーザーが明示的に合格判定 |
| 6 | 旧・指示文生成GPTを非公開化または共有停止し、旧リンクの到達不能を確認 | 完了。共有状態「自分だけ」と未認証アクセスのHTTP 404を確認 |
| 7 | ユーザーの最終承認後、旧GPTを完全削除して一覧消失・旧リンク無効化を確認 | 完了。完全削除、所有者一覧からの消失、旧リンクのHTTP 404を確認 |

全7フェーズを完了とする。PWA 4.0.1、iPhone 16実機Safari確認、同一条件による独立した新規会話3件の最終画像品質確認、旧GPTの共有停止、完全削除、所有者一覧からの消失、および旧リンクの到達不能確認はすべて合格した。

## 9. 実装状況（2026-08-12）

- PWA 3.0.0として、条件付き衣装4系統・合計144通り、髪型17種類、前髪9種類、共通55色、名前付きプリセット、コピー、Service Workerを実装済み。
- 共通55色は髪色、瞳の色、トップスの色、ボトムスの色、上下一体衣装の色で同一の名称・順序・カラーチップを使用する。
- 保存形式v3を採用し、方針どおり旧v1・v2の前回値とプリセットは移行しない。
- 自動試験26件合格。393×852 CSS px相当のレイアウト確認と、対象項目で55色が表示されることを確認済み。
- GitHub PagesへのPWA 3.0.0公開、主要資産のHTTPS配信、公開版の髪型17種類・前髪9種類・共通55色を確認済み。
- iPhone 16実機Safari確認は、2026-08-12にユーザーから「実機確認結果良好」と報告され、合格とする。
- PWA 3.0.0の基準17行を確定し、変更前の画像生成GPT設定が非公開バックアップのハッシュと一致することを確認済み。
- 独立した新規会話で初回画像を1枚生成し、ユーザー目視で基準条件との一致を確認したため初回評価を合格とする。GPT設定とPWAは変更していない。
- 同一条件による独立した新規会話3件の最終品質確認を3サイクル実施した。第1・第2サイクルの指摘を受けて画像生成GPT側だけを調整し、第3サイクル3件を2026-08-13にユーザーが明示的に合格判定した。
- 旧・指示文生成GPTはユーザーの明示的な最終承認後に完全削除した。所有者一覧で対象名称とGPT IDがともに0件であること、ログイン状態でも旧GPTが利用不能であること、Cookieを使用しない外部アクセスで旧共有リンクがHTTP 404になることを確認した。

## 10. PWA 4.0.0公開版（2026-08-12）

- 容姿と体型のプルダウンを廃止し、指示文へ「美人」「脚の長いモデル体型」を固定出力する。
- バスト、ヒップ、衣装、髪、色の選択機能は維持する。
- 表情を必須4択（真剣、喜び、怒り、悲しみ）へ整理し、真剣を初期値とする。各選択肢は強度を抑えた具体的な表情記述として出力する。
- ポーズ、構図、背景、照明は「指定なし」を維持し、画像差が比較的大きい候補だけへ整理する。
- 衣装未選択13行、上下分離18行、上下一体17行の出力規則とする。
- 保存形式v4を採用し、旧v1・v2・v3の前回値とプリセットは移行しない。
- 自動試験30件合格。ローカル画面で項目構成、初期13行、ステージ衣装・上下分離18行の連動出力を確認済み。
- 画像生成GPT側は肌表現を美しさ優先の方針へ調整済み。非公開Instructions本文は公開記録へ残さない。
- GitHubへのマージ、GitHub Pages公開、公開資産の4.0.0切り替え確認は完了。iPhone 16実機Safariで容姿・体型の非表示と表情変化を確認し、合格とする。

## 11. PWA 4.0.1公開版（2026-08-12）

- PWA 4.0.0はiPhone 16実機Safariで、容姿・体型の非表示と表情変化が合格した。
- 衣装装飾の基準語句を「容姿の印象に合わせて」から「衣装に合わせて」へ変更する。
- 衣装装飾の初期値は「内容は衣装に合わせておまかせ、装飾量は控えめ」とする。
- 自動試験31件に合格し、旧語句が指示文へ残らないことを確認する。
- マージ、GitHub Pages公開、公開資産の4.0.1切り替え確認は完了。2026-08-13にiPhone 16実機Safari確認も完了し、合格とする。

## 12. 最終画像品質確認（2026-08-13）

- PWA 4.0.1、同一の画像生成GPT設定、同一の基準18行を固定し、各サイクルで独立した新規会話3件へ投入した。
- 第1サイクルでは顔の局所的な赤みと光沢、第2サイクルでは頬骨周辺の色調差が残ったため、ユーザー承認後に画像生成GPT側だけを段階的に調整した。PWAは変更していない。
- 公開記録には非公開Instructions本文を残さず、調整内容を肌の質感、メイク、照明、顔と首の色調連続性、人体表面の過度な細部強調の抑制として要約する。
- 第3サイクル3件はユーザーの目視評価で改善が確認され、明示的な「合格」を受領したため、フェーズ5を完了とする。

## 13. PWA 6.0.0公開版（2026-08-13）

- 撮影設定をPWAの必須選択へ移し、ポーズ9種類、構図4種類、カメラアングル3種類、背景19種類、照明5種類を実装する。
- 指示文の全行を、主語と述語を持ち句点で終わる完全な文章へ統一する。
- 関連項目をグループ枠でまとめ、コピーとリセットを画面下部のアイコン操作バーへ固定する。
- 保存形式v6を採用し、旧v1～v5の前回値とプリセットは移行しない。
- 画像生成GPTは、PWAの撮影設定を上書きする通常時既定値を削除し、人物・人体・衣装の品質制御だけを担う構成へ更新する。非公開Instructions本文は公開記録へ残さない。
- 自動試験43件と393×852相当のブラウザー表示確認に合格した。公開版のiPhone 16実機Safari確認と、変更後GPTを使用した生成画像のユーザー品質判定は未実施とする。

## 14. PWA 6.0.1公開版（2026-08-14）

- 同一主語が続く行では重複する主語を省き、意味と述語を維持したまま指示文を短文化する。
- 固定操作バーをsafe area直上の画面最下部へ置き、左リセット・右コピーの均等幅とする。
- 「椅子に浅く座る」は手を膝や太腿へ固定せず、腕と手を姿勢に合う自然な位置とする。
- 保存形式v6と既存プリセットの値は維持し、出力時の変換だけを変更する。
- 自動試験43件とブラウザー確認に合格。画像生成GPTを短縮文章形式と新しい実行判定文へ整合し、GitHub Pagesへ公開した。公開後のiPhone 16実機Safari確認もユーザーの目視判定で合格した。
- PWA 6.0.1と更新済み画像生成GPTを組み合わせた生成画像試験も完了し、ユーザーの明示的な「合格」を受領したため、6.0.1の実機・画像品質確認を完了とする。

## 15. PWA 6.1.0候補（2026-08-14）

- PWAを画風非依存の人物画像指示文作成器とし、固定文からフォトリアル指定を削除する。画風は出力先の画像生成GPTが固定規則として担当する。
- カメラアングルを高さだけの3択とし、正面、斜め45度、真横、背面の撮影方向4択を独立項目として追加する。
- ポーズを12種類へ拡張し、既存ポーズを含め、成立に不要な手・腕の指定を削除する。
- 保存形式v7とキャッシュ `7days-v13` を採用し、旧v1～v6の前回値とプリセットは移行しない。
- 画像生成GPTはPWAの画風指定に依存せず、常にフォトリアル画像を生成する固定規則と、`画像を新規生成する。`を中心とする実行判定へ更新する。非公開Instructions本文は公開記録へ残さない。
- PWAの自動試験45件と393×852相当のブラウザー表示確認に合格し、画像生成GPTの設定変更も保存後の再読込で確認した。
- PR #26を`main`へマージし、GitHub Pagesのデプロイ成功を確認した。
- iPhone 16実機確認と、PWA 6.1.0と更新済みGPTを組み合わせた生成画像のユーザー品質判定は未実施とする。

## 16. PWA 6.2.0公開版（2026-08-14）

- 生成指示文を改行なしの1行へ変更する。文の順序、意味、句点は維持し、文間を半角スペースで区切る。
- 共通色を55色から110色、髪型を17種類から26種類、照明を5種類から16種類へ拡張する。
- 照明はオーソドックス、明るい、暗い、独特の4系統を各4種類とする。
- 衣装デザインを合計211種類へ拡張する。私服・その他衣装の上下一体には承認済みコンセプト衣装8種類を追加し、不採用となったステージ向けコンセプト案は収録しない。
- UI表示名と出力語句を点検し、造語または誤解を招く略称を一般的な名称へ置き換える。
- 保存形式v8とキャッシュ `7days-v14` を採用し、旧v1～v7の前回値とプリセットは移行しない。
- 自動試験46件、コミット、マージ、GitHub Pages公開に合格した。2026-08-15にiPhone 16実機Safari確認もユーザーの目視判定で合格した。さらに、同一基準指示文を写真用画像生成GPTの独立した新規会話3件へ投入し、生成画像3枚すべてがユーザーの目視判定で合格したため、6.2.0の実機・画像品質確認を完了とする。

## 17. PWA 7.0.0公開版（2026-08-15）

- 既存の写真版を維持したまま、単一の `index.html` 内へアニメ人物イラスト用モードを追加する。アニメモードの画面名は「3日くらい休んでいいのに」とする。
- 固定操作バーを左からリセット、モード切替、コピーの順とし、幅比を1:1:2とする。写真モードにはブラシ、アニメモードにはカメラの切替アイコンを表示し、ページ再読込なしで切り替える。
- 色、衣装、靴、髪型、前髪、瞳色、バスト、ヒップ、撮影方向、背景は写真版と共有する。
- アニメ版固有項目として、目の形6種類、表情16種類、ポーズ45種類、構図7種類、カメラアングル7種類、照明32種類を実装する。目の形の初期値はアーモンド型とする。
- アニメ版のPWA固定文は新規生成と架空の成人日本人女性1人の2文だけとし、美人、体型、フォトリアル、アニメ画風の固定文は出力しない。
- 写真版の保存形式v8を変更せず、アニメ版は専用保存形式v1を使用する。前回値とプリセットは両モード間で読み書き・削除しない。
- Web版選択肢カタログを廃止し、Excelを人が閲覧する正規台帳とする。
- 2ページ構成の実装前状態をリポジトリ外へ退避したうえで単一HTMLへ統合し、自動試験67件、コミット、`main`へのマージ、GitHub Pages公開、iPhone 16実機Safari確認に合格した。
- アニメ用の正式運用GPTは「アニメ人物イラスト 新規生成GPT v2」とし、ユーザーの生成画像目視確認で品質合格・正式採用とする。「アニメ人物イラスト 新規生成GPT vTest」は不合格・非採用、「アニメ人物イラスト 新規生成GPT v2.1.0.0TEST」は正式採用せず比較用として保持する。各GPTの非公開Instructions本文と管理リンクは公開記録へ残さない。
- Excel台帳の内容更新は、スプレッドシート専用編集・検証環境が利用できないため未完了とする。

## 18. PWA 8.0.0統合版（2026-08-16）

- 写真・アニメのモード切替を廃止し、画風を出力しない単一の共通指示文生成画面へ統合する。
- 目の形6種類、目の表現18種類、特殊な目10種類、口の表現24種類と、7.0.0の豊富な撮影設定を採用する。
- 衣装タイプ・構成の独立文を削除し、衣装文へ「ステージ衣装向け」または「私服向け」を含める。
- 上下分離のトップスとボトムスへ「無し」「おまかせ」を追加する。上下一体は「無し」を許可せず、「おまかせ」を初期値とする。
- 上半身用・下半身用の下着を独立項目へ移し、主衣装の有無に応じて主衣装または内側の下着として出力する。重複していた一般衣装側の下着候補は削除する。
- 統合保存形式v1を採用し、旧写真版v8の前回値とプリセットを初回だけ移行する。旧キーは削除せず、既存統合データを上書きしない。
- キャッシュは `7days-v17` とし、版を8.0.0へ更新する。
- 自動試験19件と393×852相当のブラウザー確認に合格した。GitHub Pages公開とiPhone 16実機Safari確認は公開後に行う。
